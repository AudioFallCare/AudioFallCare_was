name: Code Review

on:
  pull_request_target:
    types: [opened, synchronize]

jobs:
  review:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      pull-requests: write

    steps:
      - name: Get PR info
        id: pr-info
        uses: actions/github-script@v7
        with:
          script: |
            const { data: files } = await github.rest.pulls.listFiles({
              owner: context.repo.owner,
              repo: context.repo.repo,
              pull_number: context.payload.pull_request.number
            });

            const changedFiles = files.map(f => f.filename);
            const additions = context.payload.pull_request.additions;
            const deletions = context.payload.pull_request.deletions;
            const changedFilesCount = changedFiles.length;

            let review = [];
            review.push('## 📋 PR 자동 리뷰\n');

            // Summary
            review.push('### 📊 변경 사항 요약');
            review.push(`- 📁 변경된 파일: **${changedFilesCount}개**`);
            review.push(`- ➕ 추가된 줄: **${additions}줄**`);
            review.push(`- ➖ 삭제된 줄: **${deletions}줄**`);
            review.push('');

            // File categorization for Spring Boot
            const fileTypes = {
              controllers: [],
              services: [],
              repositories: [],
              entities: [],
              dtos: [],
              configs: [],
              tests: [],
              others: []
            };

            for (const file of changedFiles) {
              if (!file) continue;
              if (file.includes('Controller')) fileTypes.controllers.push(file);
              else if (file.includes('Service')) fileTypes.services.push(file);
              else if (file.includes('Repository')) fileTypes.repositories.push(file);
              else if (file.includes('Entity') || file.includes('entity')) fileTypes.entities.push(file);
              else if (file.includes('Dto') || file.includes('dto')) fileTypes.dtos.push(file);
              else if (file.includes('Config') || file.includes('config')) fileTypes.configs.push(file);
              else if (file.includes('Test') || file.includes('test')) fileTypes.tests.push(file);
              else fileTypes.others.push(file);
            }

            review.push('### 📂 변경된 파일 분류');
            if (fileTypes.controllers.length) review.push(`- 🎮 Controller: ${fileTypes.controllers.length}개`);
            if (fileTypes.services.length) review.push(`- ⚙️ Service: ${fileTypes.services.length}개`);
            if (fileTypes.repositories.length) review.push(`- 🗄️ Repository: ${fileTypes.repositories.length}개`);
            if (fileTypes.entities.length) review.push(`- 📦 Entity: ${fileTypes.entities.length}개`);
            if (fileTypes.dtos.length) review.push(`- 📨 DTO: ${fileTypes.dtos.length}개`);
            if (fileTypes.configs.length) review.push(`- 🔧 Config: ${fileTypes.configs.length}개`);
            if (fileTypes.tests.length) review.push(`- 🧪 Test: ${fileTypes.tests.length}개`);
            if (fileTypes.others.length) review.push(`- 📦 기타: ${fileTypes.others.length}개`);
            review.push('');

            // Warnings
            let warnings = [];
            if (additions > 500) warnings.push('⚠️ 변경량이 500줄을 초과합니다. PR을 더 작게 나누는 것을 고려해주세요.');
            if (changedFilesCount > 20) warnings.push('⚠️ 20개 이상의 파일이 변경되었습니다. PR을 더 작게 나누는 것을 고려해주세요.');
            if (fileTypes.tests.length === 0 && (fileTypes.services.length > 0 || fileTypes.controllers.length > 0)) {
              warnings.push('💡 Service/Controller 변경이 있지만 테스트 파일이 없습니다. 테스트 추가를 고려해주세요.');
            }

            if (warnings.length > 0) {
              review.push('### ⚠️ 주의사항');
              warnings.forEach(w => review.push(w));
            }

            // Post comment
            const { data: comments } = await github.rest.issues.listComments({
              owner: context.repo.owner,
              repo: context.repo.repo,
              issue_number: context.payload.pull_request.number
            });

            const botComment = comments.find(c => c.user.login === 'github-actions[bot]' && c.body.includes('PR 자동 리뷰'));

            if (botComment) {
              await github.rest.issues.updateComment({
                owner: context.repo.owner,
                repo: context.repo.repo,
                comment_id: botComment.id,
                body: review.join('\n')
              });
            } else {
              await github.rest.issues.createComment({
                owner: context.repo.owner,
                repo: context.repo.repo,
                issue_number: context.payload.pull_request.number,
                body: review.join('\n')
              });
            }
